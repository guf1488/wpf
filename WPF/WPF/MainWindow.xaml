<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid x:Name="dataGrid1" AutoGenerateColumns="False" HorizontalAlignment="Left" Height="163" Margin="114,81,0,0" VerticalAlignment="Top" Width="314">
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="Monitor" Binding="{Binding Monitor}"/>
                <DataGridTextColumn Header="Processor" Binding="{Binding Processor}"/>
                
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
